# Production Environment Variables for FluxIndexer Stack
# Copy this file to .env.production and customize the values

# Docker Configuration
DOCKER_USERNAME=yourusername
VERSION=latest

# PostgreSQL Database
POSTGRES_DB=fluxindexer
POSTGRES_USER=fluxindexer
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD

# Flux Daemon RPC
FLUX_RPC_USER=fluxrpc
FLUX_RPC_PASSWORD=CHANGE_THIS_RPC_PASSWORD

# Indexer Configuration
INDEXER_BATCH_SIZE=100
INDEXER_POLLING_INTERVAL=1000

# Logging
LOG_LEVEL=info

# Explorer Frontend API URL Configuration
# Options:
#   AUTO - Auto-detect from browser URL (recommended for FluxOS multi-node deployment)
#   http://your-ip:42067 - Use specific IP/domain
#   http://localhost:42067 - For local development
NEXT_PUBLIC_API_URL=AUTO

# Bootstrap Configuration (OPTIONAL - significantly speeds up initial sync)
# Flux Blockchain Data Bootstrap (blocks/chainstate folders - .tar.gz format)
# Leave empty to sync from genesis block
# Example: BOOTSTRAP_URL=https://your-cdn.com/flux_blockchain_data.tar.gz
BOOTSTRAP_URL=

# PostgreSQL Database Bootstrap (.pgdump or .sql.gz format)
# Leave empty to index from genesis block
# Example: DB_BOOTSTRAP_URL=https://your-cdn.com/postgresql_fluxindexer.pgdump
DB_BOOTSTRAP_URL=

# Indexer Start Height
# -1 = Start from genesis block (block 0)
# Set to specific block height if using a bootstrap (should match last block in bootstrap file)
INDEXER_START_HEIGHT=-1
